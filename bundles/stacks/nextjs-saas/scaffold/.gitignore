# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# Production
dist/

# Testing
coverage/
test-results/
playwright-report/
playwright/.cache/

# Environment files
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
.DS_Store

# Drizzle migrations are COMMITTED — they are the source of truth applied to
# prod on a fresh DB. A correct baseline (drizzle/0000_init.sql + meta) ships
# with this scaffold; `npm run db:generate` adds the next delta. Do NOT ignore
# the drizzle/ folder. (Only the live local DB dir, if any, is ignored.)

# Misc
*.log
.turbo/
