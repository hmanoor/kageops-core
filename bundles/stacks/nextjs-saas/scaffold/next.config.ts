import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // App Router is the default in Next 16; no `experimental.appDir` flag needed.
  reactStrictMode: true,
};

export default nextConfig;
